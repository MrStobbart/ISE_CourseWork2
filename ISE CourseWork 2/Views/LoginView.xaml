<Page x:Class="ISE_CourseWork_2.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISE_CourseWork_2.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="LoginPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label x:Name="lblUsername" Content="Username" MinWidth="100"/>
                <TextBox x:Name="txtUsername" Width="180" VerticalContentAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label x:Name="lblPassword" Content="Password" MinWidth="100" VerticalContentAlignment="Center" />
                <TextBox x:Name="txtPassword" Width="180" VerticalContentAlignment="Center"/>
            </StackPanel>
            <Button x:Name="btnLogin" Content="Login" Click="BtnClick_Login" HorizontalAlignment="Right" Margin="10" Width="100" />
        </StackPanel>
    </Grid>
</Page>
