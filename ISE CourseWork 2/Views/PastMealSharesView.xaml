<Page x:Class="ISE_CourseWork_2.Views.PastMealSharesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="PastMealSharesView">
    <Page.Resources>
        <CollectionViewSource x:Key="ItemCollectionViewSource"  CollectionViewType="ListCollectionView"/>
    </Page.Resources>


    <Grid>
        <DataGrid DataContext="{StaticResource ItemCollectionViewSource}" 
                  ItemsSource="{Binding}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=CookName}" Header="Cook" FontWeight="Bold"/>
                <DataGridTextColumn Binding="{Binding Path=EaterName}" Header="Eater" FontWeight="Bold"/>
                <DataGridTextColumn Binding="{Binding Path=DateTime}" Header="Data and Time" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
